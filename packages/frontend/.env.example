# Google OAuth client ID used by the Login/Register pages (@react-oauth/google).
# Without a valid value the Login page's GoogleOAuthProvider will throw
# "Missing required parameter client_id".
VITE_GOOGLE_CLIENT_ID=your-google-oauth-client-id
